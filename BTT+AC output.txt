const modifier = (text) => { 
  // BTT hook (safe if missing)
  if (typeof onOutput_BTT === "function") {
    text = onOutput_BTT(text);
  }

  // Auto-Cards hook (optional, safe if missing)
  if (typeof AutoCards === "function") {
    text = AutoCards("output", text);
  }

  return { text };
}//end of modifier

// Don't modify this part
modifier(text)
